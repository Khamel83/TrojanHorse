# Spec Tasks

## Tasks

- [ ] 1. **Create `classify.py` module**
  - [ ] 1.1 Create the `classify.py` file in the `TrojanHorse` directory.

- [ ] 2. **Implement `categorize` function**
  - [ ] 2.1 Write tests for the `categorize` function.
  - [ ] 2.2 Implement the `categorize` function in `classify.py`.
  - [ ] 2.3 Verify all tests pass.

- [ ] 3. **Implement `extract_action_items` function**
  - [ ] 3.1 Write tests for the `extract_action_items` function.
  - [ ] 3.2 Implement the `extract_action_items` function in `classify.py`.
  - [ ] 3.3 Verify all tests pass.

- [ ] 4. **Integrate with daily notes**
  - [ ] 4.1 Modify the `transcribe.py` script to call the `classify.py` module after transcription.
  - [ ] 4.2 Add a setting to `config.json` to allow users to define their own categories.
  - [ ] 4.3 Append the classification results to the daily notes file.
