# Root of your vault (e.g. an iCloud Drive subfolder)
# Example:
#   /Users/omar/Library/Mobile Documents/com~apple~CloudDocs/WorkVault
WORKVAULT_ROOT=/absolute/path/to/your/vault

# Comma-separated list of "capture" directories (relative to WORKVAULT_ROOT)
# These are where Drafts, MacWhisper, etc. will drop new files.
# Example: Inbox,TranscriptsRaw
WORKVAULT_CAPTURE_DIRS=Inbox,TranscriptsRaw

# Directory (relative to WORKVAULT_ROOT) where processed notes will be written.
# If empty, processed notes are written next to the source file.
# Example: Processed
WORKVAULT_PROCESSED_ROOT=Processed

# Directory for trojanhorse internal state (absolute path).
# Default: ~/.trojanhorse
TROJANHORSE_STATE_DIR=

# OpenRouter config
OPENROUTER_API_KEY=your_key_here
OPENROUTER_MODEL=google/gemini-2.5-flash-lite-001

# Embeddings
# Provider can be "openai" or "openrouter" (for OpenRouter embeddings)
EMBEDDING_PROVIDER=openai
EMBEDDING_MODEL_NAME=text-embedding-3-small
EMBEDDING_API_KEY=
EMBEDDING_API_BASE=https://api.openai.com/v1

# OpenRouter embedding config (if using OpenRouter as provider)
OPENROUTER_EMBEDDING_MODEL=openai/text-embedding-3-small